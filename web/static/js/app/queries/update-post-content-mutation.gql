mutation updateContent($input: UpdatePostContentPayload!) {
  updateContent(input: $input) {
    content {
      id
      body
      channels {
        id
        name
        provider
        image
      }
    }
    contentErrors {
      field
      message
    }
  }
}
