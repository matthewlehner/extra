mutation UpdateSchedule(
  $channelId: ID!
  $autopilot: Boolean
  $timezone: String
) {
  updateSchedule(
    channelId: $channelId
    schedule: { autopilot: $autopilot, timezone: $timezone }
  ) {
    id
    autopilot
    timezone
  }
}
